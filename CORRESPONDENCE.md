# Соответствие: экран прототипа → требование ТЗ → модуль бэкенда

> Документ для демонстрации заказчику: показывает, что веб-прототип `sima-web`
> покрывает все 16 требований ТЗ и что UI готов к подключению реального
> FastAPI-бэкенда (контракты в `src/api/types.ts`).

Легенда статуса бэкенда (по `ANALYSIS_REPORT.md` §10):
- ✅ реализовано в бэкенде, переиспользуется с минимальными правками;
- 🟡 частично / требует доработки;
- ❌ не реализовано в бэкенде — UI проектирует требование с нуля (опережает бэкенд).

---

## Сводная таблица

| # | Экран (route) | Требование ТЗ | Модуль бэкенда | Статус бэкенда | Что делает UI |
|---|---|---|---|---|---|
| 1 | `/upload` | Поддержка форматов АФС (GeoTIFF/BigTIFF/JPEG) | `processings/utils.py`, `raster_utils.py` | 🟡 частично | Поле каталога АФС + оценка разрешения; форматы — на бэкенд |
| 2 | `/upload` | Поддержка ВЛС (LAS) | `laspy` во всех модулях | ✅ | Поле каталога ВЛС + оценка плотности |
| 3 | `/settings`, `/upload`, `/relief` | Воспроизводимость (seed, deterministic) | — | ❌ (0 совпадений seed в репо) | Тумблер детерминизма, seed, cudnn-флаги, num_workers |
| 4 | `/tasks` | Пакет: пропуск тайла + список необработанных | `batch_predictor/predictor.py` | ❌ (try/except нет) | Очередь задач, прогресс по тайлам, failed_tiles с причинами, повтор |
| 5 | `/upload` | Оценка материалов (СК, площадь, разрешение, масштаб ОФП, плотность ВЛС, масштаб ТЛО, диапазон высот ТЛО) | — | ❌ (нет модуля) | Карточки АФС/ВЛС с метриками |
| 6 | `/upload` | Параметры съёмки независимо для ВЛС и АФС | — | ❌ | Две независимые карточки АФС/ВЛС |
| 7 | `/upload`, `/relief` | Приведение съёмок к целевой СК | `gdal.Warp` (без dstSRS) | 🟡 | Селектор целевой СК + чекбокс «привести» |
| 8 | `/relief` | Классификация точек «Рельеф» | `CMR.py` (PDAL SMRF) | ✅ | 5 методов фильтрации + параметры SMRF |
| 9 | `/relief` | Сглаживание ЦМР с параметрами | `raster_utils.gauss_smooth` | ✅ | sigma/order/window + тултип о ×разрешение |
| 10 | `/relief` | Слои: уклоны, экспозиции, TPI | `SFR.py`, `tpi.py` | ✅ | Чекбоксы + разрешения + радиусы TPI |
| 11 | `/forest` | ЦМД (CHM/ITS/DEN) | `CMD.py` | ✅ | Пороги ярусов + каналы CHM/ITS/DEN |
| 12 | `/forest` | Статистики ТЛО в сегментах крон | `statistics.py` | ✅ | Перцентили, шаг VCI, список метрик |
| 13 | `/forest` | Детекция объектов древостоя (веса заказчика) | `detector.py` (YOLOv5), `pycrown.py` | ✅ | Метод (NN/watershed/оба), тайл, перекрытие, сезон |
| 14 | `/relief` | Высота на ЦМР | `height.py` (LAS-Z, не растр) | 🟡 | Источник высот: LAS \| растр ЦМР + шаг |
| 15 | `/water` | Сегментация поверхностных вод | `segmentator.segment('water')` | ✅ | Порог, тайл, перекрытие, разрешение, сглаживание |
| 16 | `/forest` | Категория рубки (линейный алгоритм) | `set_spec_TLO.py` (пороги) | 🟡 | Переключатель «пороги» \| «линейная модель» + пороги |

---

## Экран «Задачи» (/tasks) — ответ на критический риск #4

| Элемент UI | Соответствие в бэкенде | Заметка |
|---|---|---|
| Прогресс-бар по тайлам | `predictor.py` цикл без прогресса | UI добавляет то, чего нет в коде |
| Сводка done/running/failed/queued | — | нет в бэкенде |
| `failed_tiles[]` с причиной | нет try/except в циклах | критический блокер ТЗ |
| Кнопка «Повторить» | — | нет в бэкенде |

При подключении реального бэкенда SSE-симуляция (`setInterval`) в `useJobSimulation`
заменяется на `EventSource`/WebSocket — остальная UI-логика не меняется.

---

## Экран «Настройки» (/settings) — ответ на риски безопасности и воспроизводимости

| Элемент UI | Соответствие в бэкенде | Закрывает риск отчёта |
|---|---|---|
| Пути к весам (`model_paths`) | захардкоженные креды Artifactory в `model_downloader.py` | 🔴 #1 — секрет-менеджер вместо хардкода |
| Детерминизм (seed, cudnn, num_workers) | 0 совпадений seed в репо | 🔴 #3 — воспроизводимость |
| Рабочие каталоги | локальные пути | перенос в S3/MinIO |

---

## Архитектурный замысел (из `src/api/types.ts`)

> «Моки возвращают те же типы. При переходе на реальный бэкенд меняется только
> реализация fetch-обёрток в `src/api/*.ts`.»

Контракт зафиксирован типами: `Project`, `MaterialAssessment`, `Job`,
`ReliefParams`, `ForestParams`, `WaterParams`, `ResultLayer`, `GlobalSettings`.
Эти типы 1:1 соответствуют полям бэкен‑модулей. UI **не нужно перерабатывать**
при подключении FastAPI — достаточно реализовать `src/api/*.ts` поверх тех же типов.

---

## Итог

- **8/16** требований (✅) — UI повторяет готовые параметры бэкенда (вынос хардкода в UI).
- **2/16** (🟡) — частично реализованы, UI добавляет недостающее (СК, высота на ЦМР).
- **6/16** (❌) — не реализованы в бэкенде, но **уже спроектированы в UI**:
  #3 (детерминизм), #4 (пакет+пропуск), #5/#6 (оценка материалов), #7 (СК), частично #16 (линейная модель).

Прототип доказывает: веб-перенос не теряет инженерной полноты и **опережает**
бэкенд по части требований ТЗ, задавая контракт для их реализации.