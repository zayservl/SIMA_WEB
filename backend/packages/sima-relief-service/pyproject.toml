[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "sima-relief-service"
version = "0.1.0"
description = "СИМА — сервисный слой рельефа (библиотека, готовая к обёртке в backend-сервис)"
requires-python = ">=3.9"
dependencies = [
    "numpy>=1.20",
    "scipy>=1.7",
    "laspy>=2.0",
    "rasterio>=1.2",
    "shapely>=2.0",
    "pyproj>=3.0",
    "sima-dem-core",
    "sima-dem-ground",
]

[tool.setuptools.packages.find]
where = ["src"]