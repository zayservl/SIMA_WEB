# План доработок прототипа интерфейса СИМА

> Источник: интервью с заказчиком 03.07.2026 (`Транскрипция Новая запись (2).txt`)
> + 4 требования от пользователя + текущий код `sima-web`.
>
> Главное направление: **убрать модуль визуализации**, акцент на работу с
> задачами и данными результата (статус по тайлам, потайловый перезапуск,
> проверка данных по тайлу, логика сохранения после каждого тайла).

## Решения по вопросам (согласовано)

1. Экран «Результаты» — карта заменяется целиком на менеджер данных/архивов.
2. Сессии: внутри сессии папка с `id сессии`, внутри неё — папки с `id каждого
   тайла`; при повторном запуске тайла создаётся **новый уникальный id** (старые
   выходные данные не перезаписываются).
3. Ролевую модель и авторизацию — **откладываем**; пока одна широкая роль с
   доступом ко всем функциям.
4. План хранится в этом файле.

---

## Блок A. Удаление модуля визуализации — P0
- A1. Удалить `src/pages/Results.tsx` и маршрут `/projects/:projectId/results` из `src/App.tsx`.
- A2. Убрать пункт «Результаты» из `src/components/layout/Sidebar.tsx`.
- A3. Убрать зависимость `maplibre-gl` из `package.json` и файл `public/demo-afs.png`.
- A4. Типы `LayerKind`/`ResultLayer` в `src/api/types.ts` — заменить на контракты выходных артефактов (Блок Г).

## Блок Б. Статус по каждому тайлу — P0
Расширить таблицу тайлов в `src/pages/Tasks.tsx` и модель `TileInfo`:
- Б1. Жизненный цикл: `queued → running → done | failed | skipped` + метки `started_at/finished_at/duration`.
- Б2. Прогресс внутри тайла: текущий шаг алгоритма + временная оценка.
- Б3. Кликабельная строка тайла → выезжающая панель/модал с деталями (входные файлы, СК, размер, разрешение/плотность, выходные файлы, лог по шагам).
- Б4. Фильтры/группировка таблицы по статусу + поиск по имени.

## Блок В. Перезапуск потайлово — P0
- В1. Контекстное действие на строке тайла: «Перезапустить тайл» (failed/skipped), «Остановить тайл» (running).
- В2. Групповая кнопка «Перезапустить только упавшие» на карточке задачи.
- В3. «Повторить с новыми параметрами» — открывает форму параметров, создаёт новую сессию.
- В4. Подтверждение перезапуска с предупреждением при изменении детерминизма.

## Блок Г. Куда сохранено + логика сохранения — P0
- Г1. Сессионная модель в `src/api/types.ts`: `Job` получает `session_id`, `output_dir`. Структура: `results/<project>/<job_type>/session-<id>/<tile_id>/…`. Повторный запуск тайла → новый `tile_id` (не перезаписывает).
- Г2. Дерево выходных артефактов по тайлам (ЦМР, уклоны, TPI, кроны, вода, болота, буферы…) с типом (GeoTIFF/BigTIFF/LAS/Shapefile), размером, путём.
- Г3. Кнопки: «Скачать архив тайла», «Скачать архив задачи», экспорт в хранилище.
- Г4. Индикатор промежуточных/временных файлов (раздувание диска) + статус очистки.
- Г5. Просмотр лога задачи по шагам с временными метками.

## Блок Д. Проверка данных по тайлу и свойства — P1
- Д1. В `src/pages/Upload.tsx` оценка материалов — потайловая (СК, площадь, разрешение / плотность, диапазон ТЛО).
- Д2. Валидация пары ТИФ+ЛАС: проверка совпадения СК; несовпадение → предупреждение + блок/предупреждение перед пуском.
- Д3. Свойства выходных данных по тайлу — выносится в Блок Г (Г2) и панель тайла (Б3).

## Блок Е. Сопутствующие правки — P2
- Е1. Форматы вывода: GeoTIFF/BigTIFF(>4 ГБ)/LAS 1.2/Shapefile; убрать `.jpg` из `Upload.tsx`.
- Е2. Индикатор «АФС и ВЛС приведены к X» на этапе оценки.
- Е3. Отображение seed в карточке задачи и в логе сессии.
- Е4. Ролевая модель/авторизация — отложено.

## Приоритеты
- P0: A, Б, В, Г1–Г3.
- P1: Д, Г5.
- P2: Е.